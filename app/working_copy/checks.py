"""Foundational-policy startup self-check (APP-21).

Stub module. Real logic lands in Task 3 onwards. Imported by
`app/working_copy/apps.py:WorkingCopyAppConfig.ready()` so the Django
check framework registers the check at app load time.
"""
