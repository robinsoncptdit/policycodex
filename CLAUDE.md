# CLAUDE.md

Standing context for the PolicyCodex project. Read this first when working in this folder.

*Renamed to PolicyCodex 2026-05-11 (primary domain `policycodex.org`); folder path stays `/Users/chuck/PolicyWonk/` and `PolicyWonk-*.md` filenames stay as-is. Public root carries public artifacts; `internal/` (local sprint workspace, gitignored 2026-06-08, kept locally — history still in git) holds sprint/design docs; `archive/` (gitignored) holds 2024 history.*

## What This Project Is

PolicyCodex is a tool that helps Catholic dioceses (and adjacent mission-driven nonprofits) manage the full lifecycle of their governing documents: inventory, review, approval, publication, and ongoing maintenance. Open source under a maintainer model (the maintainer earns through setup, support, and customization, not through licensing).

The active spec lives in `PolicyWonk-v0.1-Spec.md`. The sprint board lives in `PolicyWonk-v0.1-Tickets.md`. Read the spec before doing any substantive work in this folder.

## Current Status

Active. Targeting a public demo at DISC (Diocesan Information Systems Conference) mid-June 2026. Pensacola-Tallahassee is install zero; the Archdiocese of Los Angeles is the reference design partner.

Week 4 closed 2026-05-24 ahead of plan (suite 339 -> 373). **PUBLISH-07 LIVE 2026-05-26: `https://handbook.ptdiocese.org/` deploys through the three-job workflow on every merge to `pt-policy/main`; Let's Encrypt cert through 2026-08-24.** Suite at 379. **APP-15 (onboarding screen-7 bootstrap), APP-25 (foundational typed-table editor, demo slice), APP-16 (onboarding configuration commit), and APP-23 (read-only policy detail view + L1 gate) landed 2026-06-05 via subagent-driven development; suite at 435.**

**Week-5 polish-week scope:** remaining wizard screens (APP-10..16) + provisioning (APP-15/16), read-only policy detail view (APP-23), generic-ship audit + clean-VM install verification (REPO-10), Python version pin (REPO-11), `workflow_dispatch:` on shipped workflows (REPO-12), incremental re-run + full PT-corpus run (INGEST-05/06), inventory-pass orchestrator (AI-10). Hard feature freeze at end of Week 5. **Progress (2026-06-05): APP-15 done as the read-only bootstrap loop (the editable typed-table UI split out to APP-25, also shipped); APP-16 done — screen-7 accept now commits `.policycodex/config.yaml` + the bundle to the diocese repo in one PR (secrets scrubbed, scoped `git add`); APP-23 done — `/policies/<slug>/` read-only detail view (title, metadata, `provides:`, escaped body, gate badge) with the catalog's L1 foundational gate, catalog rows now link to it, browser-verified; APP-26 filed for post-DISC editor hardening + demo polish. REPO-05 done — containerized install (PT-free Dockerfile, Profile A build compose + Profile B published-image stub, `.env.example`, one-command `install.sh`), env-driven Django hardening (SECRET_KEY/DEBUG/ALLOWED_HOSTS now env-sourced, SECRET_KEY required when DEBUG off), WhiteNoise static, AGPL "View Source" footer; state in the `policycodex-data` volume, credentials bind-mounted read-only from `~/.config/policycodex/`; validated on a live docker daemon (Colima) 2026-06-07 — image builds no-cache, container boots (migrate + gunicorn), serves `/health/` 200 + `/` 302->login + static 200, SQLite persists across `down`/`up` recreate, Profile B (`docker-compose.pull.yml`) config valid; validation surfaced and fixed a Compose `env_file` `$`-interpolation bug that silently stripped `$` from `get_random_secret_key()` keys (now generate `$`-free keys, commit 0d752b9); suite 463. AI-10 done 2026-06-05 — Django-free inventory-pass orchestrator (`ai/inventory.run_inventory_pass`) walks an ingest manifest, runs generic per-policy extraction, emits `policies/<slug>.md` + `.audit.yaml` drafts, and opens ONE bulk PR on a deliberately non-slug-mapped `policycodex/inventory-<hex>` branch; re-run safe (existing slugs/bundles never clobbered), in-pass slug collisions surfaced not dropped, corrupt files non-fatal; thin Django CLI `core/management/commands/run_inventory_pass.py`; suite 490. REPO-11 done 2026-06-07 (`a76d4bb`) — pinned the supported Python: floor `>=3.12` (set by Django 6.0), standardized dev == prod on 3.14 via `.python-version`, bumped the Docker base to `python:3.14-slim-bookworm` (overrides REPO-05's 3.12 ratification), raised `app/requirements.txt` `django>=5.0` -> `>=6.0`, new `tests/test_python_pin.py` keeps the pin/base/floor in lockstep, live `docker build --no-cache` validated on Colima (3.14-slim pulled, git + collectstatic OK, runtime Python 3.14.5); suite 493. REPO-12 done 2026-06-07 (`f242198`..`9e6e342`) — `workflow_dispatch:` added to both shipped workflows so workflow-only fix-forward PRs can be triggered without a `paths:`-filter nudge; suite 495. APP-27 done 2026-06-07 (`96eae86`/`91e7e3c`) — `/htmx/` URL prefix convention laid (namespaced empty `core/htmx_urls.py` wired into `core/urls.py`), no endpoints yet (APP-28c fills it); suite 497. INGEST-05 done 2026-06-07 (`60a86ba`/`a2d9002`/`d70ea81`) — incremental re-run: pure-data `ManifestDiff` + `diff_manifests` in `ingest/manifest.py`, JSON persistence + `plan_incremental_run` walk/build/diff orchestrator in new `ingest/incremental.py` (all file I/O isolated there); orchestrator returns the change set, caller persists `diff.current` only after a successful run (crash-safe); `diff.to_process` feeds `ai.inventory.run_inventory_pass` unchanged; suite 505. INGEST-06 done 2026-06-08 (`bc3118d`/`5b65b28`) — corpus ingest verification: the 19 PT PDFs were moved out of the finished spike (`spike/inputs/`) to `~/.config/policycodex/corpus/pt/` (gitignored, local-only, matching the credentials convention); new `ingest/tests/test_corpus_integration.py` (4 tests) gated on a `POLICYCODEX_CORPUS_DIR` env var (`skipif` — runs locally, skips in CI), deliberately corpus-agnostic per ship-generic (no hardcoded "19", the count is run evidence not a committed assertion); covers walk + extract + `build_manifest` + `plan_incremental_run` (added-then-unchanged JSON round-trip), does NOT call the AI pass (offline/deterministic). Real-corpus run: 19 walked, 18 extract clean text, manifest 19 hashed, incremental added=19 then unchanged=19. **One finding: 1 of 19 (`Appendix 10.2 Compliance Memo June 2016.pdf`) is a single-page image-only scan with no text layer — pypdf legitimately extracts nothing; the test tolerates empty extraction ONLY for genuinely image-only PDFs (so a real text-bearing regression still fails). OCR is out of scope for v0.1 pre-freeze; a follow-up ticket is recommended so the inventory pass doesn't silently emit an empty policy for scanned source docs.** No product code changed; suite 505 passed + 4 corpus-gated (509 collected). INGEST-08 filed 2026-06-08 off that finding (flag image-only/scanned source docs in the inventory-pass / CLI output; detect-and-warn only, OCR deferred; post-freeze). APP-30 done 2026-06-08 (`2092cc7`/`27324ef`/`6dd85a6`) — screen-7 image-only-PDF guard: promoted the image-only signal to production as `ingest.extractors.pdf_has_embedded_images`, refactored the corpus test onto it, and added a screen-7 guard that blocks the proceed (and never calls the AI) when an uploaded retention PDF extracts to empty text, with a scan-specific vs generic "no readable text" message; suite 505 -> 513. AI-16 done 2026-06-08 (`8a37a56`..`95ddcf4`) — usage capture in the inventory audit sidecar: `LLMProvider.complete()` now returns a frozen `CompletionResult(text, usage)` wrapping a frozen `Usage(provider, model, input_tokens, output_tokens, timestamp)`; `ClaudeProvider` stamps all five at call time; `inventory_extract` attaches them as the private `_usage` dict key (mirrors `_source_file`), `audit.to_audit_yaml` renders a top-level `usage:` block after `confidence:` (null when absent), and `emit.py` whitelists `_usage` into `_PRIVATE_FIELDS` so telemetry never leaks into policy markdown; retention extraction adapts to the new return type but stays unmetered for v0.1 (no audit sidecar). Foundation for v0.2 P2.11 spend ceilings; companion APP-31 (wizard step 6 + README API-key clarity) still open. No new files; suite 513 -> 524. APP-28 done 2026-06-08 (`56260db`..`52e222d`) — Tailwind + DaisyUI build-chain + retemplate + live HTMX, shipped subagent-driven a->b->c->d (fresh implementer + two-stage review per task, final holistic review): (a) Tailwind v4 standalone CLI + DaisyUI `.mjs` plugins, toolchain gitignored under `.tools/` and downloaded by `scripts/build-css.sh`, source `styles/input.css` kept OUTSIDE the served static tree (so `collectstatic`/WhiteNoise manifest storage doesn't choke on build-only `@import`/`@plugin` tokens), only the compiled `static/css/policycodex.css` committed, structural + env-gated drift-guard tests; (b) all 12 templates (8 core + 4 onboarding) retemplated to the DaisyUI/Inter/slate vocabulary with empty-catalog state, a reusable AI-outage `alert` partial, an a11y baseline, favicon + per-page titles; (c) live HTMX under `/htmx/` — onboarding screen-7 fragment dispatch (`screen7_fragment`, running alongside the intact no-JS `handle()` fallback) with the `hx-indicator` spinner, plus the foundational typed-table row-add endpoint (`core/views.py:foundational_row` + `fragments/*_row.html`, OOB `TOTAL_FORMS` bump); (d) `tests/test_static_assets_ship.py` asserts the compiled CSS/JS ship + carry brand/component sentinels, generic-ship leak scan extended to `static/`+`styles/`, build step documented in `install.sh`+`README.md`. Suite 513 -> 544 (+5 corpus-gated skips); CSS drift guard green; production static pipeline verified over HTTP. One DoD item OPEN: the 1280x720 browser visual sweep is blocked on the Claude Chrome extension being disconnected. APP-31 done 2026-06-08 (`d9c4789`/`f51d22c`/`019bb9a`) — wizard step-6 LLM-provider screen prose + per-provider API-key doc links + a rough monthly cost table, plus a README "Before you begin" note clarifying that PolicyCodex needs API access (an Anthropic/OpenAI/Gemini/Azure key), not a consumer chat subscription. APP-29 done 2026-06-08 (`75f5a35`/`44b55f9`) — onboarding wizard completion screen: a presentation-only GET view (`app/onboarding/views.py:onboarding_complete` + a pure `_derive_repo(state)` helper) that replaces the post-onboarding `redirect("catalog")` and walks the admin through the publish sequence (merge the config PR -> configure GitHub Pages -> set the registrar CNAME `handbook -> {org}.github.io`); derives the repo/Pages/CNAME/HOWTO links from screen-1 wizard state plus a session-stashed PR url, no API calls or commits; both onboarding accept exits (plain-POST `redirect` + HTMX `HX-Redirect` 204) reroute to it; route registered before the `<slug:step>/` catch-all; ships in the APP-28 DaisyUI/Inter vocabulary; suite 558 -> 564. **With APP-29 done the APP-27 -> APP-28 -> APP-29 demo-UI chain is complete and the v0.1 pre-freeze functional set is closed; the only carried-forward gap is the 1280x720 browser visual sweep for APP-28/29/31, still blocked on the Claude Chrome extension being disconnected.** **Integrated golden-path code review 2026-06-08 (suite verified green first-hand at 564): no Critical issues, the demo golden path holds end to end. It surfaced a fix list, now tracked as new tickets: APP-32 (the foundational typed-table editor HARD-deletes classifications, violating the locked soft-delete guarantee), REPO-14 (the L2 CI guard never inspects `data.yaml`, so L1+L2 are no-ops for classification content), AI-17 (the INGEST-05 incremental seam is NOT wired into the inventory command, so a changed source doc is silently not re-processed and the documented "`diff.to_process` feeds `run_inventory_pass`" claim is currently false), APP-33 (a failed git step leaves the working copy dirty on `main` and can wedge later syncs -- recommend fixing pre-DISC), and APP-34 (minor UI polish). None block the demo, but APP-32 + REPO-14 need a fix-or-rescope decision before the four-layer deletion-protection story is repeated at DISC, and a live classification-delete should stay out of the demo script until then.** REPO-15 done 2026-06-08 — SQLite default now persists to `/data` when the container volume is mounted (explicit `POLICYCODEX_DB_PATH` still wins; the ephemeral in-container fallback footgun is fixed); suite 564 -> 566. **Full read-only codebase audit 2026-06-09 (policycodex-auditor skill): suite verified green first-hand at 566. Findings filed as REPO-16..REPO-20 + APP-35..APP-37, and scope widened on APP-32 (`deprecated: true` is unrepresentable end-to-end: initial loader, form, and extraction whitelist all drop it) and APP-33 (the SUCCESS path also never checks the working copy back out to the default branch). Pre-DISC fix list: APP-35 (compiled CSS stale behind the APP-29 completion screen), REPO-16 (`.env` baked into the Docker image), REPO-17 (LLM key not wired from the `/secrets` mount), APP-33 (+checkout-back), REPO-19 (README login step).** REPO-16 done 2026-06-09 (`0790e5e`) — `.env`/`.env.*` excluded from the Docker build context (the `COPY . .` was baking the seeded SECRET_KEY into image layers), regression-guarded; suite 567. APP-35 done 2026-06-09 (`13d5226`) — compiled CSS rebuilt (Tailwind v4.3.0/DaisyUI 5.5.23) so the APP-29 completion + APP-31 provider screens render styled; new toolchain-free canary in `tests/test_static_assets_ship.py` pins one class per newest template; drift guard re-verified green; suite 568. APP-33 fully done 2026-06-09 (`c9b52e0`/`2e519b3`/`d11f2c9`/`3b9a0b6`) — the `propose_change` service from `c9b52e0` is now wired into all three call sites: `policy_edit`, `_foundational_edit_post`, and `finalize_onboarding`. Every write path (policy edit, foundational typed-table edit, onboarding accept) writes its file then funnels through `propose_change`, which restores a clean default branch on ANY failure (revert the write, remove untracked created paths, delete the local branch) and returns the working copy to the default branch on success — so a failed git step can no longer leave `main` dirty (the next `WorkingCopyManager.sync()` `git pull` can no longer wedge) and a successful PR no longer leaves the working copy stranded on the feature branch. The exception catch widens from `(RuntimeError, ValueError)` to `Exception` at all three sites, so a real `CalledProcessError`/`ConnectionError` from the provider no longer escapes to a 500. The misleading "the change is saved locally" wording dropped (propose_change reverted it). Test fixtures `git init` the working copy where they exercise propose_change end-to-end; `keeps_local` failure assertions became `with_clean_tree` assertions. Suite 568 -> 573. REPO-17 done 2026-06-09 (`b60a587`/`cfd6f0f`/`33ab29c`/`22ab7fe`) — LLM API key now flows from `/secrets/config.env` into the container process env. New `docker/load-secrets.sh` POSIX helper does `set -a; . "$SECRETS_FILE"; set +a` against the bind-mounted (or `POLICYCODEX_CONFIG_PATH`-overridden) file. `docker/entrypoint.sh` sources it before `migrate`/`gunicorn` with `|| true`, so a malformed user-side line stays logged-but-non-fatal under `set -e`. `Dockerfile` ships the helper with `chmod +x` next to the entrypoint COPY. `INSTALL-WITH-CLAUDE.md` spells out `ANTHROPIC_API_KEY=sk-ant-…` (and the OpenAI/Gemini/Azure equivalents) in the credentials section and adds the helper to the maintainer file list, so the existing lockstep guard catches future drift. Zero Python changes; every provider SDK's native env-name is automatically honored. No-op when `/secrets/config.env` is missing, so first-run admins can complete login + the wizard up through step 6 before configuring credentials; the screen-7 AI-outage handler already covers the call-time degrade. Three subprocess tests (`tests/test_load_secrets.py`) drive the helper directly; two structural guards (extended in `tests/test_install_prompt_sync.py` and `tests/test_docker_packaging.py`) keep the entrypoint, Dockerfile, and install prompt in lockstep. Suite 573 -> 578. REPO-19 done 2026-06-10 (`14033a0`/`614db4c`) — README install walkthrough now routes the first-run admin through `/login/` first, with the credentials source spelled out: the plain-Python Quick Start gains `.venv/bin/python manage.py createsuperuser` between `migrate` and `runserver`; the Docker subsection expands the `.env.example` breadcrumb to mention `DJANGO_SUPERUSER_USERNAME / _EMAIL / _PASSWORD` and adds a parenthetical `docker compose exec app python manage.py createsuperuser` Alternative fallback (framed as mutually exclusive, not additive, after the code-quality review). New `tests/test_install_prompt_sync.py::test_readme_install_walkthrough_documents_login_step` pins `/login/` + `createsuperuser` to the README install section by slicing between `## Quick Start` and `## Roadmap`, mirroring the existing `_ROUTE_TOKENS` contract that already guards `INSTALL-WITH-CLAUDE.md` for the same routes; the module docstring widened to cover both docs. Zero product-code change (`@login_required` on `/onboarding/`, `/catalog/`, `/` was already correct; only the docs lagged); `INSTALL-WITH-CLAUDE.md` unchanged (Phase 4 + Phase 8 already covered login-first, so sync-contract verification was read-only). Suite 578 -> 579.

For sprint-by-sprint detail and per-wave narrative, see `internal/PolicyWonk-Daily-Log.md` (append-only event log) and the `internal/PolicyWonk-Week-N-Demo.md` files.

## What's In This Folder

**Public root (tracked, public-facing):**

- `PolicyWonk-v0.1-Spec.md` is the active PRD.
- `PolicyWonk-v0.1-Tickets.md` is the engineering sprint board.
- `README.md` is the public-facing GitHub README.
- `LICENSE` is the AGPL-3.0 text.
- `HOWTO-GitHub-Team-Setup.md` is a generic, diocese-agnostic guide for upgrading a GitHub org to Team tier, enabling branch protection, optionally requiring the foundational-policy guard, and publishing the handbook at a custom subdomain.
- `INSTALL-WITH-CLAUDE.md` is a copy-and-paste prompt a non-technical diocesan IT admin gives to Claude Code to install and run the Docker container end to end (connect to a VM, install Docker, configure `.env` + credentials, point at policy files, build/boot, log in, run onboarding). **It mirrors the live Docker install path and MUST be updated in the same change whenever any of these moves: `Dockerfile`, `docker-compose*.yml`, `docker/entrypoint.sh`, `install.sh`, `.env.example`, the `~/.config/policycodex/` credentials convention, the onboarding/login URL routing, or the inventory-pass command.** Its "For maintainers" footer lists the exact tracked files.
- `repo-template/` holds generic, vendorable files copied into a diocese's policy repo during onboarding: the L2 foundational-policy CI guard (REPO-09), the handbook build-and-deploy workflow + vendored Astro `handbook/` + `sync-handbook.sh` re-vendor script (PUBLISH-06 + PUBLISH-07), and a README. Its `tests/` run in the main suite but are not copied on install. Canonical home for installable diocese-repo automation.
- `ai/` is the LLM provider abstraction + Claude implementation, markdown/YAML emit, confidence-audit sidecar, Django-free foundational-bundle loader, gap detection, generic per-policy extraction + the Django-free inventory-pass orchestrator (`inventory_extract.py` + `inventory.py`, AI-10), per-diocese taxonomy seeds, and tests.
- `app/` is App-lane Django code: git provider (clone/branch/commit/push/PR), local working-copy manager + L3 startup self-check, onboarding wizard (skeleton + screen 1 + per-screen form registry), and tests.
- `ingest/` is Ingest-lane code: `LocalFolderConnector` CLI, extractors (PDF/DOCX/MD/TXT), `BundleAwarePolicyReader` (reads flat policies + foundational bundles as one inventory), source manifest model, and tests.
- `core/` is the project-wide Django app: `/health/`, `/login/`, `/logout/`, policy catalog + read-only detail view, git-author mapper, the `run_inventory_pass` management command (AI-10 Django wiring), and tests.
- `manage.py` plus `policycodex_site/` is the Django 5+ project skeleton (SQLite default; `SECRET_KEY` hardening deferred per REPO-05).
- `pytest.ini` wires pytest-django for the whole repo.
- `spike/` is the riskiest-assumption extraction spike: `extract.py` loading PT taxonomy, per-policy JSON outputs (gitignored), and the `spike/eval/` regression harness.
- `.github/workflows/` holds two dev-time GitHub Actions (installed via `/install-github-app`, 2026-06-05): `claude.yml` (the `@claude` assistant, on issue/PR mentions) and `claude-code-review.yml` (auto code-review on every PR via `claude-code-action` + the marketplace `code-review` plugin). These are development tooling, not shipped product, and no-op without the `CLAUDE_CODE_OAUTH_TOKEN` secret; REPO-10 (generic-ship audit) decides whether they stay in the diocese-facing clone. Editing the review workflow has two non-obvious constraints: any change must live on `main` AND be byte-identical on the PR branch, or `claude-code-action`'s app-token exchange 401s ("Workflow validation failed"); and the review only posts when the prompt passes the PR as a full `https://github.com/...` URL plus a `--comment` flag. Full debug history in the Daily Log (2026-06-05 16:30 PT).

**`internal/` (local sprint workspace, gitignored 2026-06-08 — kept locally, never distributed; history retained in git):**

- `internal/PolicyWonk-Daily-Log.md` is Scarlet's append-only event log.
- `internal/PolicyWonk-Open-Questions.md` is the live OQ tracker.
- `internal/PolicyWonk-Week-N-Sprint-Plan.md` and `internal/PolicyWonk-Week-N-Demo.md` are per-week plans and demo wraps.
- `internal/PolicyWonk-Spike-Plan.md` is the spike with results.
- `internal/PolicyWonk-Framework-Evaluation.md`, `internal/PolicyWonk-SSG-Evaluation.md`, and `internal/PolicyWonk-Prompt-Architecture-Decision.md` capture decision rationale.
- `internal/REPO-03-GitHub-App-Checklist.md` and `internal/REPO-04-PT-Repo-Settings.md` are per-diocese onboarding checklists.
- `internal/superpowers/specs/` and `internal/superpowers/plans/` hold agent-led design and execution plans.

**`archive/` (gitignored, local-only):** 2024 feature brief, original Notes-1..9, pre-PRD Project Summary, README draft. Historical context only; not part of the public repo.

`.obsidian/` is the Obsidian vault config (gitignored). This vault opens in Obsidian. Keep new files in markdown.

## How To Approach Work Here

1. Treat the v0.1 spec and tickets as the active plan. The 2024 notes in `archive/` are historical context only.
2. The Git-backed architecture is settled. Do not propose alternative storage models without new information.
3. New sprint logs, decision rationale, and per-diocese checklists go in `internal/`. New public-facing artifacts (docs about the product itself) go at the root or under `ai/` / `app/`.
4. Naming: free-form descriptive names are fine.

## What Has Already Been Reconsidered and Locked

Do not reopen these without new information:

**Business model**
- Maintainer mode: open source plus services revenue (setup, support, customization)
- Single-tenant self-hosted VM per diocese, not multi-tenant SaaS
- License: AGPL-3.0 (resolved 2026-05-11). Canonical text committed as `LICENSE`.

**Wedge and audience**
- v0.1 wedge is inventory cleanup plus handbook publication
- Buyer and admin user is the diocesan IT director
- Distribution channel is DISC (the diocesan IT director community)
- Diocese zero is Pensacola-Tallahassee, design partner is Archdiocese of Los Angeles

**Architecture**
- Git-backed: every policy is a markdown file in a private GitHub repo per diocese
- Every gate transition is a pull request state (Drafted = open, Reviewed = approved, Published = merged)
- Handbook publishes via GitHub Actions on every merge to main
- Non-technical editors (CFO, HR director) never see Git; the admin web app handles commits and PRs on their behalf
- GitHub.com is the v0.1 default, Git provider abstracted for GitHub Enterprise, GitLab, Gitea later

**Conventions**
- Versioning address: LA Archdiocese chapter-section-item by default, Catholic healthcare department-code as an alternative, configurable per diocese
- Document version: semver (1.0 first published, 1.1 minor, 2.0 obligations changed)
- Required metadata on every published policy: owner, effective date, last review, next review, retention period (modeled on ISO 30301)
- Three gates in v0.1: Drafted, Reviewed, Published. Full Seven-Gate configurability is v0.2

**Tech**
- Claude is the default LLM. Provider abstracted for OpenAI, Gemini, Azure OpenAI, and local Llama
- Multi-LLM consensus is overbuilt for the problem. One model plus a sharp rubric plus a human approver wins
- Custom Python MCP servers are not needed. Skills (markdown plus reference docs) cover the AI-assist work
- Seven-screen onboarding wizard: GitHub repo, address scheme, versioning, reviewer roles, retention, LLM provider, source-of-truth reference documents
- UI framework: Tailwind CSS + DaisyUI + HTMX over Django templates, Inter typeface, brand color via DaisyUI CSS variables (resolved 2026-06-05 in OQ-13). Production build via the Tailwind standalone CLI (single static binary, no Node). All four runtime dependencies (Tailwind, DaisyUI, HTMX, Inter) are MIT or equivalent so AGPL stays compatible. Commercial component libraries (Tailwind UI, MUI Pro, Ant Design Pro, DevExpress, Telerik, Kendo UI) are off the table as long as PolicyCodex is AGPL. Rationale: `internal/PolicyWonk-UI-Framework-Decision.md`.

**Frontend portability constraints (preserve the option to swap the frontend stack later)**
- Page views and HTMX fragment views stay thin. Business logic lives in Django models, managers, and service functions, so a future SPA replaces only the views.
- HTMX endpoints are URL-segregated under `/htmx/`. A future JSON API at `/api/v1/` does not collide. The fragment endpoints retire cleanly when a SPA arrives.
- Authentication and authorization sit in middleware and view decorators, not in template logic, so a future JSON API gets the same protection without rework.

**AI extraction grounding (validated by spike)**
- The AI inventory pass receives two pieces of injected context per extraction: (1) the chosen address taxonomy, (2) any source-of-truth reference documents the diocese has designated
- The Document Retention Policy is the canonical source-of-truth example. It supplies retention periods, document-type taxonomy, and gap-detection signal in one document
- Without that context, retention extraction quality drops to 0.144 on the rubric. With it, the extraction passes 60% acceptance comfortably

**Foundational-policy bundle pattern (approved 2026-05-13)**
- Policies that are simultaneously inventory documents AND sources of app configuration variables (the Document Retention Policy is the canonical example) live as a directory bundle: `policies/<slug>/policy.md` (narrative + frontmatter declaring `foundational: true` and `provides: [...]`) plus `policies/<slug>/data.yaml` (canonical machine-readable variables). Both reviewed in the same PR; the app reads `data.yaml` directly with no markdown parsing.
- Live sync via PR merge: a CFO edit to the retention policy opens a PR, reviewer approves, publisher merges; the handbook rebuilds AND the app's local working copy pulls AND the next AI extraction sees the new taxonomy.
- Four protection layers against accidental deletion: L0 Git branch protection (existing), L1 app UI hides Delete (APP-20), L2 pre-merge CI guard (REPO-09), L3 app startup self-check (APP-21).
- Removing a classification is **soft-delete only** (`deprecated: true` keeps the id valid for existing references). Hard remove requires re-classifying dependents first. No auto-re-extraction of orphaned policies in v0.1.
- Non-data-bearing policies stay as flat `policies/<slug>.md` files. The bundle pattern is opt-in via the `foundational: true` frontmatter flag.
- Other source-of-truth reference docs that are NOT policies in the inventory (e.g., a category cheatsheet) may still live in a `references/` directory; the bundle pattern is for documents that play both roles.
- Full design: `internal/PolicyWonk-Foundational-Policy-Design.md`.

**Ship generic, never PT-flavored**
- v0.1 ships as a generic, diocese-agnostic codebase. PT-specific scaffolding present in the repo today (taxonomy YAML, retention PDF reference, "Diocese of Pensacola-Tallahassee" in code/comments) is development-time convenience only (install-zero has to develop against something concrete).
- Install-N flow is: clone the public repo, run install, complete the seven-screen onboarding wizard. The wizard supplies all per-diocese values, including the uploaded retention PDF that AI-parses into the diocese's own foundational-policy bundle (per APP-15-revised). No code edits required for a new diocese.
- Per-diocese values that must be wizard-driven (never hardcoded): diocese name, GitHub org/repo, classification taxonomy and retention schedule, address scheme, versioning convention, reviewer roles, retention defaults, LLM provider, other source-of-truth reference documents.
- When designing new code, treat any value that varies per diocese as wizard-sourced. The classification/retention seed (renamed to `ai/taxonomies/seed_classification.example.yaml` by REPO-10, 2026-06-05) is a clearly-labeled development fixture, not framework data; the live taxonomy lives in a diocese's policy repo as `policies/document-retention/data.yaml` (its proper home per the bundle pattern), read by capability via AI-12-revised. The seed is used only by the extraction spike as a fallback when no bundle is present.
- Code comments, error messages, log lines, class names: "the diocese" not "PT." Internal docs in `internal/` are exempt; they naturally center PT because that's the install-zero context and they aren't part of the shipping artifact.
- Tracked as REPO-10 for the Week 5 polish-week audit pass. "Install verification on a clean VM" (Week 5 calendar item) is the generic-ship test: a clean VM clones, wizards through, and operates without seeing PT anywhere.

**Design principle**
- Opinionated by default. Configurable where dioceses have legitimate variation. AI-assisted throughout.

## Style Preferences

- Spartan, clear, active voice
- No em dashes
- Bullet lists are fine in artifacts and reference docs
- "You" and "your" when addressing the reader
- No filler words
