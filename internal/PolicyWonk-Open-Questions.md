# PolicyCodex Open Questions

Source-of-truth log for blockers and pending human decisions during the v0.1 sprint. Scarlet writes this; Chuck reads, decides, and confirms in chat.

## Active

_None. All open questions are resolved as of 2026-05-24; see Resolved below._

## Resolved

| ID | Question | Resolution |
|---|---|---|
| OQ-13 | UI framework for the Django admin app (CSS framework, JS sprinkle, and component vocabulary). | Resolved 2026-06-05. **Tailwind CSS + DaisyUI + HTMX**, Inter typeface, brand color via DaisyUI CSS variables. Production build via the Tailwind standalone CLI (single static binary, no Node). Three guard rails landed alongside: (1) PRD section "Frontend Portability Constraints" (thin views + `/htmx/` URL segregation + auth in middleware); (2) PRD Licensing subsection "Frontend Dependencies and AGPL Compatibility" (allowed-license list; commercial component libraries named as disallowed); (3) the views-stay-thin / portability note written directly into `CLAUDE.md` (Tech subsection) during this pass. Architecture-hygiene follow-through is **APP-27** (formalize the `/htmx/` URL prefix in `core/urls.py`; the `CLAUDE.md` note is already done, so only the URL convention remains). Build-chain enablement (vendor the Tailwind CLI, retemplate the 8 Django templates, update REPO-10's clean-VM verification) is **APP-28**. Rationale + mockup comparison: `internal/PolicyWonk-UI-Framework-Decision.md` + `internal/mockups/disc-demo-{pico,bootstrap,tailwind}.html`. |
| OQ-05 | LA contact's role in README | Resolved 2026-05-23: Chuck decided the README credits named individuals (not institution-level only). **David Schmitt (IT Director, Archdiocese of Los Angeles)** is credited as "reviewer"; **Marcus Madsen (Director of IT, Archdiocese of Baltimore)** added as "design reviewer." Both named in the README Acknowledgments; Status line updated to "Design reviewed by the Archdioceses of Los Angeles and Baltimore." Chuck confirmed both consent to public credit. Repo confirmed PUBLIC during this pass; the stale README "Not yet public" status line was corrected in the same edit. Contact details for both stay in Scarlet's auto-memory, not the repo. |
| OQ-10 / REPO-08 | Upgrade PT GitHub org from Free to Team tier to enable private-repo branch protection | Resolved 2026-05-24: org `Diocese-of-Pensacola-Tallahassee` upgraded to **Team** (1 seat, ~$4/month). The pre-existing `main` ruleset (created REPO-04, 2026-05-11) now enforces. **Verified read-only via the GitHub API:** plan=team; ruleset enforcement=active on the default branch with require-PR, 1 approval, dismiss-stale-reviews, conversation-resolution, linear-history, block-force-push, block-deletion, and required-signatures. Satisfies PRD G3 audit trail; REPO-09 (L2 CI guard, this week) now lands on a protected repo. Residual: a live direct-push-rejection test is still worth running (Chuck), and the `handbook-build` required status check gets added when PUBLISH-06 lands this week. |
| OQ-08 | PT policy corpus exported to a local folder (full inventory beyond spike's 19 PDFs) | Resolved 2026-05-24 (Chuck, scope decision): **v0.1 ingest is local-folder only; the v0.1 corpus is the 19 spike PDFs already in a local folder.** No bulk export and no cloud connectors (SharePoint, Google Drive, OneDrive, network drives) in v0.1; those defer to v0.2 (roadmap "Drive parity"; INGEST-02 connector interface stays Stretch). Owner: Chuck; done today. Rationale: do not sink v0.1 time into connector integrations. `LocalFolderConnector` (INGEST-01) already covers this path. **Caveat:** AI eval ground-truth stays at the ~19-document sample for v0.1 (no scale-up this cycle). **Reframes INGEST-06** (Week 5) from "full PT corpus run" to "run against the 19-PDF local folder." |
| OQ-06 | PT diocesan leadership sign-off for handbook subdomain | Resolved 2026-05-23 (PT IT sign-off): **Go.** Diocesan leadership approved publishing the public handbook. Subdomain: **`handbook.ptdiocese.org`**. DNS owner: **Chuck Robinson** (makes the record change). Feeds PUBLISH-07 (Week 5 live-subdomain deploy); PUBLISH-06 (Actions build, this week) is unaffected. Subdomain also recorded in `internal/REPO-04-PT-Repo-Settings.md`. |
| OQ-01 | License: MIT, Apache 2.0, or AGPL? | Resolved 2026-05-11: AGPL-3.0. PRD updated with a new Licensing section; canonical AGPL-3.0 text committed as `LICENSE` (REPO-01). |
| OQ-04 | Monolithic vs split prompt (AI-04/05/06) | Resolved 2026-05-12: **KEEP MONOLITHIC** (Chuck signed off on Plan subagent recommendation). AI-04/05/06 reframed as eval-set tickets against the monolithic prompt; AI-11 + AI-12 layer in as additive context. Plan B if retention <0.70 after AI-12: split retention sub-prompt. Rationale in `internal/PolicyWonk-Prompt-Architecture-Decision.md`. |
| OQ-07 | PT GitHub org availability or creation | Resolved 2026-05-11: PT lives at `https://github.com/Diocese-of-Pensacola-Tallahassee`. REPO-04 unblocked. |
| OQ-03 | Web framework choice (APP-01) | Resolved 2026-05-11: **Python + Django** (per Plan subagent recommendation in `internal/PolicyWonk-Framework-Evaluation.md`). APP-01 skeleton + APP-02 unblocked. |
| OQ-09 | Static-site generator (PUBLISH-01) | Resolved 2026-05-11: **Astro** (Chuck overrode the Explore subagent's Hugo recommendation). Driver: parish web teams forking the handbook theme will find Astro's component model more approachable than Hugo's Go templates. PUBLISH-01 build-it work + PUBLISH-02 URL scheme unblocked. Subagent rationale and trade-offs preserved in `internal/PolicyWonk-SSG-Evaluation.md` for the record. |
| OQ-02 (prior form) | "PolicyWonk" trademark availability | Resolved 2026-05-11. PolicyWonk did not clear; project renamed to PolicyCodex. Reopened as the new TESS search on PolicyCodex (classes 9 and 42); see below. |
| OQ-02 | TESS search on PolicyCodex in classes 9 and 42, before first public push. | Resolved 2026-05-11. Chuck ran the USPTO TESS search on "PolicyCodex" and "Policy Codex" in classes 9 (downloadable software) and 42 (SaaS). **No wordmark hits.** First-public-push trademark gate clear. No registration filed yet; revisit if the project ever monetizes under the PolicyCodex name. |
| OQ-11 | Verify INGEST-01's symlink-skip behavior on Python 3.12. | Resolved 2026-05-13. The reviewer's premise was incorrect: raw `Path.rglob("*")` does NOT descend dir-symlinks on 3.11, 3.13, or 3.14 (verified on Chuck's macOS dev machine with the actual `LocalFolderConnector`). The Python 3.13 change merely formalized the long-standing default by adding `recurse_symlinks=False` as an explicit kwarg; the behavior itself didn't change. 3.11 brackets 3.12 from below, 3.13 brackets it from above. Both pass the new `test_walk_does_not_descend_into_dir_symlinks` regression guard added to `ingest/tests/test_local_folder.py`. Python 3.12 was not installed locally, but the bracketing evidence plus the rglob-signature continuity is sufficient. INGEST-02 / INGEST-03 cleared to proceed without an os.walk refactor. |
| OQ-12 | Fresh git worktrees fail two `spike/eval/test_run_eval.py` tests because `spike/outputs/*.json` is gitignored and not carried over. | Resolved 2026-05-14. Option (a) taken: un-gitignored the 18 canonical extraction outputs and committed them as eval fixtures. `.gitignore` rule changed from `spike/outputs/` to `spike/outputs/*` + `!spike/outputs/*.json` (root) and the redundant nested rule removed from `spike/.gitignore`. Spike-run summary artifacts (`results.csv`, `results.xlsx`) stay ignored. Fresh worktrees and clones now have everything `spike/eval/run_eval.py --offline` needs. All 5 wired fields (category, owner_role, effective_date, last_review_date, retention_period_years) still score 1.000 against the committed outputs. 116 tests passing. PT-content reversibility caveat acknowledged at decision time (option-1 commit with planned mitigation through REPO-10 if legal objects later); see Week-3 daily log. |

## Conventions

- Open questions require a human decision Scarlet cannot make alone.
- Update an entry's row when status changes; move to Resolved only when the decision is final and acted on.
- Add a row when a new blocker surfaces during execution.
