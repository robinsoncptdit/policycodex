# PolicyCodex v0.1 Engineering Tickets

*P0 work broken into sprint-board tickets, mapped to the four lanes. Architecture is Git-backed: every policy is a markdown file in a private GitHub repo per diocese, every edit is a commit, every gate is a PR state, every publish runs in GitHub Actions.*

*A subset of policies are **foundational** — their structured contents (classifications, retention schedule) drive app configuration. These live as `policies/<slug>/` bundles (`policy.md` + `data.yaml`) and get four layers of deletion protection. Design captured in `internal/PolicyWonk-Foundational-Policy-Design.md` (approved 2026-05-13). Tickets that reference this design: AI-11, AI-12, INGEST-07, APP-15, APP-20, APP-21, REPO-09.*

## Sizing Convention

- **S** = 1 to 2 days of focused work
- **M** = 3 to 5 days
- **L** = 1 to 2 weeks

## Sprint Calendar

Six weeks, four lanes. Hard scope freeze at the end of Week 2.

| Week | Theme |
|------|-------|
| 1 | Lane setup, local folder ingest reading PT corpus, GitHub App registered, first AI extract committed to the policy repo |
| 2 | App skeleton reading from local working copy, first chapter render, **scope freeze** |
| 3 | PR-backed edit flow end to end, full handbook generation in Actions, wizard skeleton |
| 4 | Full PT corpus run, wizard complete (six screens), real subdomain live |
| 5 | Bug bash, README polish, install verification on a clean VM |
| 6 | DISC prep, last-mile fixes, public announcement coordination |

## Cross-Cutting / Repo

| ID | Title | Size | Week | Notes |
|----|-------|------|------|-------|
| REPO-01 | Decide license (MIT, Apache 2.0, or AGPL) and add LICENSE file to PolicyCodex repo | S | 1 | **Blocking.** All hands sign off. |
| REPO-02 | README skeleton (status, quick start, design principles, Git-backed architecture, acks) | S | 1 | Use draft from `PolicyWonk-README-Draft.md` |
| REPO-03 | Register a PolicyCodex GitHub App for delegated diocese installs | S | 1 | **Blocking.** Owner: App lane. |
| REPO-04 | Create the PT diocesan policy repo (private) and configure branch protection. **Status:** partial; repo + ruleset configured, enforcement gated on REPO-08. | S | 1 | Owner: IT director (PT) plus App lane support |
| REPO-05 | Docker Compose and one-command install script. Must produce both (a) a Compose file that pulls a published image — the boxed-ship Profile B path for non-developer dioceses — and (b) a Compose file that builds from source (Profile A). Container image must be PT-free; per-diocese config enters via the wizard and persists in a mounted volume. AGPL footer "View Source" link to the public repo lands as part of this work. See [`project_boxed_ship_container`](#) memory + REPO-10 audit. Image publishing to a registry is a post-DISC follow-up. **Resolved 2026-06-05 (`24e9c6e`..`98ef26e`): env-drove SECRET_KEY/DEBUG/ALLOWED_HOSTS/DB path + a configurable `POLICYCODEX_SOURCE_URL` through a pure `policycodex_site/env.py` helper (folds in the deferred SECRET_KEY hardening; DEBUG defaults off, SECRET_KEY required when off). Added WhiteNoise so static serves with DEBUG off. AGPL "View Source" footer via a `source_url` context processor; login page now extends `base.html` for a single-source footer. PT-free `Dockerfile` (`python:3.12-slim-bookworm`, installs git, build-time collectstatic, gunicorn entrypoint), `.dockerignore`, `docker/entrypoint.sh` (migrate + gated createsuperuser + gunicorn). `docker-compose.yml` (Profile A, builds from source) and `docker-compose.pull.yml` (Profile B, `ghcr.io/policycodex/policycodex:latest` stub, not yet published). State persists in the `policycodex-data` volume (`/data`); credentials bind-mount read-only from host `~/.config/policycodex/` at `/secrets` (zero code change: `github_config.py` already honors `POLICYCODEX_CONFIG_PATH`). `.env.example` + one-command `install.sh` (docker preflight, seed `.env`, refuse empty SECRET_KEY, `compose up --build`). Structural guards in `tests/test_docker_packaging.py`; extended `tests/test_generic_ship.py` to scan root packaging files. NOT validated against a live docker daemon (none in the build env): `docker compose config` + the image build must be run manually before DISC. REPO-11 ratifies the `python:3.12-slim-bookworm` base pin. Registry publishing remains post-DISC. Suite 436->463.** | M | 4-5 | App or Publish lane owns |
| REPO-06 | CONTRIBUTING.md naming the configurable-vs-opinionated split | S | 5 | Single owner |
| REPO-07 | Issue and PR templates on the PolicyCodex repo | S | 5 | Single owner |
| REPO-08 | Upgrade `Diocese-of-Pensacola-Tallahassee` GitHub org from Free to Team tier (~$4/user/month) so branch protection on private `pt-policy` becomes enforceable. Tracked previously as OQ-10. **Must close before week 4 lane acceptance** to satisfy PRD G3 (audit trail). Chuck + PT IT director action. | S | 4 | REPO-04 |
| REPO-09 | Pre-merge CI guard for foundational policies on the diocese policy repo (GitHub Action). Block any PR diff that (a) deletes a file declaring `foundational: true` in its frontmatter, or (b) empties out a declared `provides:` capability. L2 protection layer per `internal/PolicyWonk-Foundational-Policy-Design.md`. Installed once per diocese repo (likely scaffolded by APP-04 / REPO-04). | S | 3-4 | REPO-04, APP-15 |
| REPO-10 | Generic-ship audit pass: codebase sweep to remove all PT-specific hardcoding before public push. Scope: (1) grep `app/`, `core/`, `ai/`, `ingest/`, `policycodex_site/` for "PT", "Pensacola-Tallahassee", "pt_", "PT_" — any hits in non-test, non-internal code must route through the wizard / settings / `data.yaml` instead; (2) confirm `ai/taxonomies/pt_classification.yaml` has either moved to PT's policy repo as a foundational bundle OR is clearly labeled as a test fixture; (3) confirm error messages, log lines, comments use "the diocese" generically; (4) clean VM install-verification run (clone, `docker compose up`, wizard through, exercise the edit flow) sees no "PT" / "Pensacola" / "internal/" leakage anywhere user-visible. Auditor records each finding + resolution in a small REPO-10 worksheet. Anchors the Week-5 polish-week deliverable; "install verification on a clean VM" IS this test. **Resolved 2026-06-05 (`0355597`..`1d07d71`): the live read paths were already generic (AI-12-revised moved taxonomy reads to the foundational bundle, by capability not slug), so the audit found only three residual string leaks. Codified the grep as a permanent regression guard `tests/test_generic_ship.py` (scans the six shipping roots incl. vendored `.sh`/`.ts`/`.css`, excludes test files; denylist of `pensacola`/`tallahassee`/`pt-policy`/`pt_classification` + a narrow `internal/` dev-doc-path regex). Fixed the three leaks: renamed `ai/taxonomies/pt_classification.yaml` -> `seed_classification.example.yaml` (generic dev-fixture header, spike re-pointed), genericized the E002 startup-check hint, genericized the vendored build-handbook preflight comment. Ran the non-Docker clean-VM verification (fresh clone -> venv -> pip install -> migrate -> runserver; Python 3.14.5): `/onboarding/`, `/catalog/`, `/health/`, and the startup warning all render generically with zero PT/internal leakage. `docker compose up` variant deferred to REPO-05; Python pin folds into REPO-11. Worksheet: `internal/REPO-10-Generic-Ship-Audit.md`. Suite 435->436.** | S | 5 | All wizard tickets (APP-08..16), AI-12-revised |
| REPO-11 | Pin the supported Python version. The repo declares no Python version anywhere today (no `.python-version`, `runtime.txt`, or `python_requires`), so installs build against whatever `python3` the host provides; on a host defaulting below 3.12 the Django 6.0 stack will not install. Declare the floor (>=3.12, set by Django 6.0) and a target version, and add the pin where it is load-bearing for reproducible installs: the Docker base image (REPO-05), a `.python-version` and/or `python_requires`, and the clean-VM install path (REPO-10). Decide whether to standardize dev on a stable 3.12 / 3.13 vs. the current bleeding-edge 3.14 (which already required a `@dataclass` / `sys.modules` workaround in REPO-09's tests). Filed 2026-05-24. **Resolved 2026-06-07 (`a76d4bb`): floor is `>=3.12` (set by Django 6.0); standardized dev == prod on Python 3.14 (Chuck's call) rather than dropping to a stabler minor, so the REPO-09 3.14 `sys.modules`/`@dataclass` workaround stays load-bearing. Added `.python-version` (`3.14`); bumped the Docker base image `python:3.12-slim-bookworm` -> `python:3.14-slim-bookworm` (deliberately overrides REPO-05's note that REPO-11 would "ratify" the 3.12 base); raised `app/requirements.txt` `django>=5.0` -> `>=6.0` so the `>=3.12` floor is justified by the declared Django floor. Declared via `.python-version` only (no `pyproject.toml`/`python_requires`: this is a Django app, not a distributed package). README already documents "Python 3.12+" prose. New structural guard `tests/test_python_pin.py` keeps `.python-version` <-> Dockerfile base <-> django floor in lockstep. Live `docker build --no-cache --pull` validated on Colima 2026-06-07: `python:3.14-slim-bookworm` pulled, apt `git` (2.39.5) installed, pip install + build-time collectstatic (130 files) succeeded, image runtime confirmed Python 3.14.5. Suite 490->493.** | S | 5 | REPO-05, REPO-10 |
| REPO-12 | Add `workflow_dispatch:` to the shipped `repo-template` workflows (`build-handbook.yml`, `foundational-guard.yml`) so a maintainer can manually re-trigger a run without a path-touching dummy commit. The current `paths:` filters (`policies/**` and `handbook/**` for build-handbook; `policies/**` for foundational-guard) do NOT match `.github/`-only changes, so fix-forward PRs to the workflows themselves do not re-fire the build. This gap surfaced twice during installs: Node-24 PR #4 (2026-05-24) and PUBLISH-07's preflight `pages:read` fix-forward (2026-05-26 PR #6 -> PR #7); each time a one-line `handbook/README.md` HTML-comment touch was needed to nudge the trigger. With `workflow_dispatch:` declared, `gh workflow run build-handbook.yml --ref main` would do the same job without an extra PR + bypass-and-restore cycle. Scope: add the trigger to both workflows in `repo-template/.github/workflows/`, extend the template tests (`repo-template/tests/test_build_handbook.py` and the foundational-guard equivalent) to assert the trigger is present, update `HOWTO-GitHub-Team-Setup.md` with a one-line "to re-run manually after a workflow-only change: `gh workflow run build-handbook.yml --ref main`" pointer. Filed 2026-05-26. **Resolved 2026-06-07 (`f242198`/`5d3c720`/`9e6e342`): `workflow_dispatch:` added to both shipped workflows (with an inline comment naming the manual-run command), one trigger-assertion test added per workflow (`test_workflow_allows_manual_dispatch` in `test_build_handbook.py`; `test_workflow_triggers_on_pull_request_and_manual_dispatch` — the first workflow-YAML test in `test_foundational_guard.py`), HOWTO Part 4 Step 4 pointer added. Inline execution per the plan `internal/superpowers/plans/2026-06-07-repo-12-workflow-dispatch.md`. Suite 493 -> 495.** | S | 5 | None |

## Ingest Lane (P0.1)

Scope per the v0.1 PRD: **Local folder ingest only.** Native SharePoint, OneDrive, Google Drive, Box, and Dropbox connectors are deferred to v0.2 per P1.2. Any export from those systems lands in a folder of files that PolicyCodex can ingest.

| ID | Title | Size | Week | Depends on |
|----|-------|------|------|-----------|
| INGEST-01 | Local folder reader: walk a directory recursively, accept path via CLI argument or config | S | 1 | None |
| INGEST-02 | Connector interface so v0.2 cloud connectors (SharePoint, OneDrive, Drive, Box, Dropbox) can drop in alongside `LocalFolderConnector` (P1.2 framework) | S | 1-2 | INGEST-01 |
| INGEST-03 | File content extraction for PDF, DOCX, MD, TXT | M | 1-2 | None |
| INGEST-04 | Source manifest data model (path, hash, last-modified, source label) | S | 2 | None |
| INGEST-05 | Incremental re-run support (skip unchanged files via hash comparison). **Resolved 2026-06-07 (`60a86ba`/`a2d9002`/`d70ea81`).** Subagent-driven (plan `internal/superpowers/plans/2026-06-07-ingest-05-incremental-rerun.md`); three TDD task-commits, fresh implementer per task on `main`, controller-verified each diff against spec. Pure-data `ManifestDiff` + `diff_manifests` added to `ingest/manifest.py` (classifies current-vs-prior by content hash into added/changed/unchanged/removed; `.to_process` = added+changed, `.current` = the manifest to persist). New `ingest/incremental.py` holds all file I/O (honoring manifest.py's "no persistence" contract): `save_manifest`/`load_manifest` (JSON; missing file -> `[]` so a first run reads everything as new) + `plan_incremental_run(root, manifest_path, source_label)` (walk -> build -> load prior -> diff). Orchestrator deliberately does NOT save; caller persists `diff.current` AFTER processing succeeds, so a crash mid-run never records unprocessed files as already-seen. `diff.to_process` is a `list[ManifestEntry]` that drops straight into `ai.inventory.run_inventory_pass(manifest=...)` unchanged. 8 new tests (incl. the acceptance case: edit one file, only that file re-queues). Suite 497 -> 505. | S | 3 | INGEST-04 |
| INGEST-06 | Test ingest against the v0.1 PT corpus (the 19 spike PDFs) in a local folder. Per OQ-08 (2026-05-24) the v0.1 corpus is the 19 PDFs; no larger export. Generic 50+-file scale target stays covered by P0.1. **Resolved 2026-06-08 (`bc3118d`/`5b65b28`).** Subagent-driven (plan `internal/superpowers/plans/2026-06-08-ingest-06-corpus-ingest-verification.md`); corpus moved to `~/.config/policycodex/corpus/pt/` (gitignored, local-only). New `ingest/tests/test_corpus_integration.py` (4 tests) gated on `POLICYCODEX_CORPUS_DIR` (skips in CI); corpus-agnostic per ship-generic. Covers walk, extract, `build_manifest`, and `plan_incremental_run` (added-then-unchanged round-trip). Real-corpus run: 19 PDFs walked; 18 extract clean text; 1 image-only scan (`Appendix 10.2 Compliance Memo June 2016.pdf`) tolerated as a known limitation (OCR out of scope v0.1 — follow-up ticket recommended); manifest 19 hashed entries; incremental added=19 then unchanged=19. No product code changed. Suite 505 passed + 4 corpus-gated. | S | 4 | All Ingest tickets |
| INGEST-07 | Bundle-aware policy reader: when `policies/<slug>/` is a directory containing `policy.md` (frontmatter declares `foundational: true` and `provides: [...]`) plus `data.yaml`, treat the directory as one logical policy in the inventory. Non-data-bearing policies stay as flat `policies/<slug>.md` files. See `internal/PolicyWonk-Foundational-Policy-Design.md`. | S | 3 | INGEST-03 |
| INGEST-08 | Flag image-only / scanned source documents during ingest and warn the user, rather than silently emitting an empty policy. Scope is **detect-and-warn only — NOT OCR** (OCR is deferred post-v0.1). When extraction yields empty/whitespace-only text for a file that is a genuinely image-only PDF (no text layer, has embedded images), surface a clear per-file warning in the inventory-pass output / CLI and skip or mark that document instead of producing an empty draft. Found by INGEST-06 (2026-06-08): 1 of the 19 PT corpus PDFs (`Appendix 10.2 Compliance Memo June 2016.pdf`) is a single-page scan that pypdf cannot read. The verification test already classifies image-only PDFs via `_empty_extraction_is_explained` in `ingest/tests/test_corpus_integration.py` — reuse that signal. | S | 5 | INGEST-06, AI-10 |

**Lane acceptance (week 4):** Given a local directory containing the v0.1 PT corpus (the 19 spike PDFs; OQ-08), the ingest returns a structured manifest with source paths, content hashes, and timestamps in under 5 minutes. Re-running against the same directory with one file changed re-processes only the changed file. Running against a missing or empty directory fails with a clear error naming the offending path. (The generic 50+-file scale target lives in spec P0.1.)

## AI Lane (P0.2)

| ID | Title | Size | Week | Depends on |
|----|-------|------|------|-----------|
| AI-01 | LLM provider abstraction interface | M | 1 | None |
| AI-02 | Claude implementation of the provider interface | S | 1 | AI-01 |
| AI-03 | Stub implementations for OpenAI, Gemini, Azure, local Llama | S | 5 | AI-01 |
| AI-04 | Category extraction eval set against the monolithic prompt (OQ-04 resolved 2026-05-12: monolithic kept; deliverable is labeled eval set + scoring harness + regression baseline, not a separate prompt file) | S | 1-2 | AI-02 |
| AI-05 | Owner, effective date, review date, retention extraction eval sets against the monolithic prompt | M | 2 | AI-02, AI-14 |
| AI-06 | Chapter-section-item address suggestion eval set against the monolithic prompt. **Carried forward from Week 2.** AI-11 (Week 2) injected the PT taxonomy but committed without an address eval set (the seed it produced violated the AI-14 schema; see 2026-05-13 review). AI-06 properly delivers it: 18 verified or `needs_review` rows under the AI-14 hardened schema, scored against the post-AI-11 outputs. | S | 3 | AI-02, AI-11 |
| AI-07 | Confidence scoring on all extraction outputs. **Scope resolved 2026-05-24: the extraction prompt already scores every field (low/medium/high); the v0.1 deliverable is the separate audit file the spec mandates (spec line 99). Build an `ai/` audit emitter (parallel to `ai/emit.py`) that writes per-policy confidence to a `<slug>.audit.yaml` sidecar. Do NOT add confidence to policy.md (AI-08 strips it per spec). No detail-view dependency; UI badges defer to APP-23.** | S | 3 | AI-04, AI-05, AI-06 |
| AI-08 | Markdown plus YAML front matter emitter | S | 2 | AI-04, AI-05, AI-06 |
| AI-09 | Wire AI-suggest buttons into the onboarding wizard | S | 4 | APP-08 (wizard skeleton) |
| AI-10 | Inventory pass orchestrator: runs all extractions on a manifest, commits markdown to the diocese policy repo as initial drafts | M | 3 | All AI tickets, APP-04 (Git ops) | **Resolved 2026-06-05 (`878dfd2`..`85d8da8`): the Django-free orchestrator `ai/inventory.run_inventory_pass` walks an ingest manifest, runs the generic per-policy extraction (`ai/inventory_extract.py`, mirrors `retention_extract.py` but with no PT-flavored strings), emits `policies/<slug>.md` + `policies/<slug>.audit.yaml` drafts into the working copy, then opens ONE bulk PR on a `policycodex/inventory-<8hex>` branch. Four locked design decisions: (1) bulk branch is deliberately NOT slug-mapped (mirrors the onboarding init branch) so `branch_to_slug` makes the catalog gate ignore the import PR; (2) re-run safe — a slug whose flat `.md` OR bundle dir already exists is skipped, never clobbered, protecting human edits + the foundational bundle; (3) in-pass slug collisions (two source files slugifying to one name at 50+ scale) are surfaced as a distinct `skipped_collision` bucket in the PR body, not silently dropped; (4) one corrupt/unsupported file is non-fatal — it lands in `errors`/`skipped_unsupported` and the bulk pass proceeds. Thin Django CLI `core/management/commands/run_inventory_pass.py` does the wiring (loads working-copy config, foundational taxonomy by capability, walks the local folder → manifest). Built via subagent-driven development, two-stage review per task. Suite 463→490 (+27). Plan: `internal/superpowers/plans/2026-06-05-ai-10-inventory-pass-orchestrator.md`.** |
| AI-11 | Inject diocese's chosen address taxonomy into the extraction prompt context. **Resolved 2026-05-13.** For PT (install zero), the taxonomy is sourced from PT's own Document Retention Policy (Section 3.0 + Appendix A). Landed as `ai/taxonomies/pt_classification.yaml` + prompt injection in `spike/extract.py` + a `--outputs` / `POLICYCODEX_EVAL_OUTPUTS` flag on the eval harness. AI-04 category eval still scores 1.000 (no regression). The address eval seed was withdrawn at review (Critical issues); AI-06 (Week 3) delivers it. LA chapter list deferred to when LA onboards. | S | 1 | AI-04 |
| AI-12 | Wire AI extraction to read `policies/document-retention/data.yaml` from the local working copy of the policy repo (the foundational-policy bundle scaffolded by APP-15). Replaces "hardcoded path to PT retention policy PDF" — the bundle pattern from the 2026-05-13 foundational-policy design (`internal/PolicyWonk-Foundational-Policy-Design.md`) means the structured retention schedule already lives in YAML. AI-11 already lands the taxonomy at `ai/taxonomies/pt_classification.yaml`; AI-12 moves the read location to the policy repo and lets edits flow live. Resolves the 0.144 retention score from the spike. **Shrinks M→S** because PDF parsing moved upstream to APP-15. | S | 3 | AI-05, INGEST-07 |
| AI-13 | Gap-detection pass: flag any policy whose type is not represented in the diocese's retention schedule. **Resolved 2026-05-24 (`bd4ef9c`): merged. v0.1 interpretation = flag a policy whose `category` is not in the bundle `classifications` (by id/name, case-insensitive); the free-text `retention_schedule` rows are not used for per-policy matching (unreliable in v0.1). Surfaced as a catalog count banner + per-row badge via the Django-free `ai/gap_detection.py` (reuses `taxonomy_loader`); degrades to off when there is no bundle. Design: `internal/superpowers/specs/2026-05-24-ai-13-gap-detection-design.md`.** | S | 3 | AI-12 |
| AI-14 | AI-04 followup: harden eval harness before AI-05. Strict `label_status` validation (raise on unknowns), row-shape validation (missing keys distinguishable from null), try/except around per-row fetch in both `--offline` and `--live` modes so one failure does not discard the rest of the run, decide per-row vs per-field `label_status` schema before a second JSONL exists, move `BASELINE_THRESHOLD` into the dispatch table as a per-field value, add comparator unit tests (`_int_eq`, `_iso_date_eq` happy/unhappy/null) and a threshold-boundary test, add `spike/eval/README.md` covering invocation and how to add rows. Drop or rename the dead `extracted_category` / `human_score` columns in `category_eval.jsonl`. **Must land before AI-05 starts** so the schema and error handling don't get retro-fixed under time pressure. | S | 2 | AI-04 |
| AI-15 | Label or drop the 4 `needs_review` rows in `spike/eval/category_eval.jsonl`. **Resolved 2026-05-13** (delegated from Chuck): Appendix 16 verified as HR, Appendix 19 verified as HR, Appendix 2 verified as "Parish Operations" (new vocab entry), Document Retention Policy dropped (now the source-of-truth reference doc per the foundational-policy design, not an inventory policy). Eval set now 17 verified rows, 0 needs_review. | S | 2 | AI-04 |

**Lane acceptance (week 4):** Given a manifest of 50+ files, the inventory pass produces matching markdown files with complete YAML front matter, commits them to the diocese policy repo on a draft branch, and opens a single bulk PR. AI suggestion acceptance rate is measurable against a human-reviewed PT subset.

## App Lane (P0.3 + P0.4 + P0.6)

| ID | Title | Size | Week | Depends on |
|----|-------|------|------|-----------|
| APP-01 | Web framework choice and project skeleton | S | 1 | None. **Blocking decision.** |
| APP-02 | Basic local user auth and identity-to-Git-author mapping | S | 1 | APP-01 |
| APP-03 | Git provider abstraction interface | S | 1 | None |
| APP-04 | GitHub provider implementation: clone, branch, commit, push, open PR, read PR state | M | 1-2 | APP-03, REPO-03 |
| APP-05 | Local working copy management (clone on first run, pull on cadence) | S | 2 | APP-04 |
| APP-06 | Catalog list view reading from local working copy of the policy repo | M | 2 | APP-01, APP-05 |
| APP-07 | Edit form for a single policy: opens a branch, commits, opens PR | M | 2-3 | APP-04, APP-06 |
| APP-08 | Onboarding wizard skeleton (seven-screen flow) | M | 3 | APP-01 |
| APP-09 | Wizard screen 1: GitHub repository (create new or connect existing). **Resolved 2026-05-24 (`7350386`): merged capture-only. Screen 1 validates + persists the repo choice (connect-existing URL vs create-new org/name + branch) to `WizardState` via a new slug->form registry (`app/onboarding/forms.py`, `GitHubRepoForm`) that the generic step view binds/validates/persists. It does NOT clone or create the repo: that provisioning is deferred to wizard completion (APP-15/16), deviating from the sprint-plan note ("calls GitHubProvider.clone + setup") for safety since the screen is back-navigable. Anchors the per-screen pattern for APP-10..16. Design: `internal/superpowers/specs/2026-05-24-app-09-wizard-github-repo-design.md`.** | S | 3 | APP-08, APP-04 |
| APP-10 | Wizard screen 2: address scheme picker | S | 3 | APP-08 |
| APP-11 | Wizard screen 3: versioning convention picker | S | 3 | APP-08 |
| APP-12 | Wizard screen 4: reviewer roles and required approvers (writes branch protection rules) | S | 4 | APP-08, APP-04 |
| APP-13 | Wizard screen 5: retention defaults | S | 4 | APP-08 |
| APP-14 | Wizard screen 6: LLM provider picker | S | 4 | APP-08 |
| APP-15 | Wizard screen 7: upload retention policy PDF, run AI extraction to produce a draft `data.yaml` (8 classifications + retention schedule, per the bundle schema), admin reviews via typed table UI, scaffold `policies/document-retention/` as the diocese's first foundational policy (policy.md with `foundational: true` and `provides: [classifications, retention-schedule]`, data.yaml, and archived source.pdf). Replaces prior scope of "point at retention policy"; the structured-data step is what the AI extraction (AI-11/AI-12) and the app UI consume. **Grows S→M** to absorb PDF parse + admin review UX. See `internal/PolicyWonk-Foundational-Policy-Design.md`. **Resolved 2026-06-05 (`f5c2fc1`..`0db4078`): shipped as the read-only bootstrap loop — upload PDF → new retention-bundle extraction `ai/retention_extract.py` (`extract_retention_bundle` + `build_data_yaml`, distinct from the single-policy `spike` extractor) → read-only review (typed-table EDITING moved to APP-25) → scaffold `policies/document-retention/` into the working copy via `app/onboarding/scaffold.py` (round-trips through `BundleAwarePolicyReader`); commit-to-repo deferred to APP-16. Screen-7 handler `app/onboarding/retention_policy.py` stages the draft on disk, runs extraction synchronously, and degrades gracefully on a bad PDF / AI outage. Extraction + scaffolder folded in as tasks, not separate tickets (AI-13 id already used by gap-detection). Scaffold path is `working_dir/policies` (matches catalog read). Suite 379→399. Plan: `internal/superpowers/plans/2026-06-05-app-15-retention-bundle-bootstrap.md`.** | M | 4 | APP-08, INGEST-03 |
| APP-16 | Configuration commit: persist wizard choices as a config file in the policy repo. **Note (2026-06-05, from APP-15): scope this commit's `git add` to `policies/` + the config file specifically — do NOT `git add .` — so the onboarding staging dir `<working_dir>/.policycodex-staging/` and the raw uploaded `source.pdf` are never swept into the diocese repo.** **Resolved 2026-06-05 (`d30478d`..`d9e11f9`): screen-7 `accept` now commits `.policycodex/config.yaml` (wizard choices, secret-named fields scrubbed) + the APP-15 `policies/document-retention/` bundle to the diocese repo in ONE PR via the established `branch→commit→push→open_pr` (mirrors `core/views.py:policy_edit`). New Django-free `app/onboarding/finalize.py` (`build_config_yaml`/`write_config_file`/`make_onboarding_branch_name`/`finalize_onboarding`). Commit is scoped to exactly `[config_path, bundle_dir]` (never `git add .`); the bundle's archived `source.pdf` lands by design but the `.policycodex-staging/` scratch + raw upload never do. Branch `policycodex/onboarding-<hex>` is deliberately not slug-mapped (catalog gate lookup ignores the repo-init PR). Provider failure degrades gracefully (bundle kept local, step not completed, review re-rendered, no 500). `repo-template/.gitignore` vendors a `.policycodex-staging/` ignore. Suite 413→422. Plan: `internal/superpowers/plans/2026-06-05-app-16-onboarding-config-commit.md`.** | S | 4 | All wizard tickets |
| APP-17 | PR-state-to-gate mapping: Drafted (open), Reviewed (approved), Published (merged) | S | 3 | APP-04, APP-07 |
| APP-18 | Approve action in UI calls GitHub review API on behalf of authenticated reviewer | S | 3 | APP-04 |
| APP-19 | Publish action in UI merges PR (requires merge permission) | S | 3 | APP-04 |
| APP-20 | Foundational-policy frontmatter recognition + L1 UI gate. App reads `foundational: true` and `provides: [...]` from each policy's frontmatter at catalog-load time. For any foundational policy, the catalog and detail views hide or disable the Delete button and show a "this policy is foundational; edit through the typed-table UI" banner. First protection layer against accidental deletion. See `internal/PolicyWonk-Foundational-Policy-Design.md` for the 4-layer model. **Scope resolved 2026-05-24: catalog-only L1 gate. The "frontmatter recognition" half is already satisfied by INGEST-07 (`LogicalPolicy.foundational`/`.provides`) plus the existing `(foundational)` badge, and APP-07 already 403s foundational edits server-side; no Delete button or detail view exists yet, so APP-20 ships the catalog mutate-affordance gate + banner and the detail-view L1 gate splits to APP-23.** Plan: `internal/superpowers/plans/2026-05-24-app-20-l1-ui-gate.md`. | S | 3 | APP-06, INGEST-07 |
| APP-21 | App startup self-check (L3 protection layer). At Django app boot, validate that every `provides:` capability the app needs (`classifications`, `retention-schedule`) is satisfied by exactly one published foundational policy in the local working copy. On miss or invalid `data.yaml`, refuse to serve and show a clear error pointing at the broken file. See `internal/PolicyWonk-Foundational-Policy-Design.md`. | S | 3 | APP-05, INGEST-07 |
| APP-22 | Extract the duplicated `_resolve_repo` subprocess invocation across `clone`, `branch`, `commit`, `push`, and `pull` in `app/git_provider/github_provider.py` into one shared helper. Tiny refactor, no behavior change, net test count unchanged. Flagged by three separate Wave-3 reviewers (APP-17 / APP-18 / APP-19 quality). Cross-cutting App-lane hygiene; added to the board 2026-05-24 per the Week-4 sprint plan. **Resolved 2026-05-24 (`8c89680`): the ticket's method list was inaccurate. The actual duplication was `_resolve_repo` (get-url -> parse -> `get_repo`) across the 5 PR methods (`open_pr`, `read_pr_state`, `list_open_prs`, `approve_pr`, `merge_pr`) plus the bare get-url in `push`/`pull`; `clone`/`branch`/`commit` were untouched. Extracted `_resolve_repo` + `_origin_url`, net -49 lines, behavior identical.** | S | 4 | APP-04, APP-17 |
| APP-23 | Read-only policy detail view + L1 gate. Add a `/policies/<slug>/` view rendering a single policy (title, frontmatter, `provides:`, body, gate state) and replace the dead `#slug` catalog-row anchor with a real link to it. Apply the same L1 foundational gate as the catalog: foundational policies show the typed-table-editor banner and no edit/delete affordance; non-foundational show an Edit link. Carved out of APP-20 (2026-05-24), which scoped to catalog-only because no detail view existed. Future home for AI-07 confidence badges and the APP-15 typed-table editor link. **Resolved 2026-06-05 (`f4e478e`..`7cb05c0`): `core/views.py:policy_detail` reusing `_find_policy` + `_build_gate_lookup` (404 on unknown slug, gate degrades to `published`), new `core/templates/policy_detail.html` (title, metadata, `provides:`, escaped `<pre>` body — no markdown lib, the Astro handbook is the rendered surface — and the catalog's L1 gate mirrored: foundational → typed-table banner + `foundational_edit` link, no flat-edit; else → `policy_edit` link), `policies/<slug:slug>/` route registered last, catalog `#slug` anchor rewired to the real link. Presentation-only gate; server-side 403 stays in `policy_edit`. Suite 422→435; final review READY; browser-verified in Chrome (flat + foundational detail + 404). Plan: `internal/superpowers/plans/2026-06-05-app-23-policy-detail-view.md`.** | S | 5 | APP-20, INGEST-07 |
| APP-25 | Foundational typed-table editor (demo slice). Editable classifications + add/edit/delete retention rows for the `document-retention` bundle, written back to `data.yaml` and opened as a PR through the existing Drafted→Reviewed→Published gate (`approve_pr`/`publish_policy` unchanged; the `policycodex/edit-<slug>-<hex>` branch convention lets `branch_to_slug` route the PR). Reachable from the catalog (the foundational row's static banner became an "Edit (typed table)" link). Reuses `ai/retention_extract.build_data_yaml` as the single `data.yaml` writer (DRY with APP-15). The "edit it live" second act of the DISC demo — peers evaluating their own systems want to see a change flow through the gates, not a one-shot import (Chuck, 2026-06-05). **Resolved 2026-06-05 (`0d83455`..`d5e57b4`): `core/views.py:foundational_edit` + `_foundational_edit_post`, Django formsets in `core/forms.py`, `foundational_edit.html`. Full per-row CRUD hardening + APP-23 reuse + demo polish deferred to APP-26. Plan: `internal/superpowers/plans/2026-06-05-app-25-foundational-editor-demo-slice.md`.** | M | 5 | APP-15, APP-20 |
| APP-26 | Post-DISC: foundational editor hardening + demo polish. Carved from APP-25 (2026-06-05), which shipped the demo slice. Scope: (1) title-case the typed-table column headers + add per-field labels (currently raw schema keys — reads as raw-schema on a projector); (2) pagination/virtualization for a full ~150–240-row schedule rendered on one page; (3) soft-delete (`deprecated: true`) semantics for a classification still referenced by other policies (per the foundational-policy design); (4) optimistic-concurrency / mtime guard on `data.yaml` (shares `policy_edit`'s single-admin no-reload assumption — also leaves a dirty working copy if the PR open fails after the local write); (5) reuse the editor from the APP-23 detail view. **Client-side multi-row add moved to APP-28c (2026-06-05) — it's one of the two live-HTMX demo interactions, so it ships pre-DISC, not post-DISC.** | L | post-DISC | APP-25, APP-23 |
| APP-27 | UI framework architecture hygiene (OQ-13 follow-through). Add an `/htmx/` URL prefix to `core/urls.py` so a future JSON API at `/api/v1/` does not collide; no HTMX endpoints exist yet, so this lays the convention before they land. Resolves OQ-13's architecture-hygiene piece. **The companion "views stay thin / HTMX URL-segregated / auth in middleware" note was already written into `CLAUDE.md` (Tech subsection) during the 2026-06-05 OQ-13 resolution pass, so that half is done; only the `/htmx/` URL convention remains.** Build-chain enablement (vendor Tailwind standalone CLI, retemplate the 8 Django templates, REPO-10 clean-VM update) is APP-28, not here. See `internal/PolicyWonk-UI-Framework-Decision.md`. **Resolved 2026-06-07 (`96eae86`/`91e7e3c`): convention laid as a dedicated namespaced module `core/htmx_urls.py` (`app_name = "htmx"`, empty `urlpatterns`) wired into `core/urls.py` via `path("htmx/", include("core.htmx_urls"))` after `health/`. No endpoints (per the convention-only scope); APP-28c fills the namespace, and future fragments reverse as `htmx:<name>`. The `policies/<slug>/` route only matches `policies/`-prefixed paths, so no collision; URLconf loads clean and the `htmx` namespace is registered. Two tests in `core/tests/test_htmx_urls.py`. Inline execution per `internal/superpowers/plans/2026-06-07-app-27-htmx-url-prefix.md`. Suite 495 -> 497.** | S | 5 | None |
| APP-28 | Tailwind + DaisyUI build-chain enablement, template adoption, and live HTMX (OQ-13 follow-on). **(a) Build chain:** vendor the Tailwind standalone CLI binary into the repo or scripted-download it at install; wire it into `manage.py collectstatic` or a pre-commit step to emit `policycodex.css`. Prove it on `base.html` + `catalog.html` as a browser-verified vertical slice before fanning out — DaisyUI classes can't be eyeballed until a stylesheet is served. **(b) Cosmetic retemplate:** retemplate the **12** existing Django templates (8 in `core/templates/`, 4 in `app/onboarding/templates/onboarding/`; the prior "8" count predates APP-23/25) to the Tailwind + DaisyUI + Inter vocabulary (slate palette, brand color via DaisyUI CSS variables). **(c) Live HTMX (both demo interactions):** wire the two mockup interactions as HTMX fragment endpoints under the `/htmx/` prefix — PDF upload → live extraction (onboarding screen 7), and typed-table client-side row-add (foundational editor; pulled out of APP-26 item 2, 2026-06-05). **(d) REPO-10 harness:** update the clean-VM verification harness to cover the new build step so the generic-ship install premise still holds. **Scope grew from M (retemplate-only) when Chuck chose "look + live HTMX" (2026-06-05); the live-HTMX layer runs tight against the end-of-Week-5 feature freeze.** Hard dependency on APP-27: the live-HTMX endpoints in (c) need the `/htmx/` prefix to exist first. **Definition-of-done clarifications (readiness pass 2026-06-08; stays one whole ticket, implement a->b->c->d): (b) also adds an empty-catalog state + an AI-outage error banner, an a11y baseline (contrast, form labels, focus order) verified at 1280x720 projector resolution in the browser-check, and a favicon + real per-page `<title>` (base.html is generic today); (c) the PDF-upload->extraction interaction MUST show an HTMX `hx-indicator` progress spinner (seconds-long extraction looks frozen on a projector without it) — a stated requirement, not a discovered one.** See `internal/PolicyWonk-UI-Framework-Decision.md` ("What did not get filed"). | L | 5 | APP-27 |
| APP-29 | Wizard completion screen ("next steps to publish your handbook"). New Django view + template rendered at **wizard completion** (i.e. once the onboarding PR is *opened*), replacing the current `redirect("catalog")` at `app/onboarding/retention_policy.py:199`. **Note (readiness pass 2026-06-08): NOT "after the PR merges" — the wizard opens the PR via `finalize_onboarding` and has no merge callback; the screen renders at wizard-end and *instructs* the admin through the merge.** Shows: the policy repo URL with a copy button, the exact DNS CNAME target the diocese must set at their registrar (`<diocese-org>.github.io`), a deep link to the diocese's GitHub Pages settings page (`https://github.com/<org>/<repo>/settings/pages`), and a link into `HOWTO-GitHub-Team-Setup.md` for the full sequence. The screen lays out the real ordering the diocese must follow: merge the onboarding PR -> Settings->Pages configure (source + custom domain) -> set the registrar CNAME (the shipping `repo-template/.github/workflows/build-handbook.yml` only deploys once Pages is configured). DNS/Pages guidance confirmed correct against that workflow (it IS GitHub Pages: `actions/deploy-pages@v5`, reads `.cname`; Pages auto-provisions the Let's Encrypt cert). Presentation only: no API calls, no commits, no GitHub Pages API. Closes the disconnect between "wizard finished" and "handbook online" that today requires alt-tabbing to docs. Bigger wizard-managed handbook publishing (subdomain collected inside the wizard, `CNAME` committed automatically) is intentionally NOT in scope here; that ships in v0.2 per PRD P2.7. Template ships in the Tailwind + DaisyUI vocabulary so the APP-28(b) retemplate pass does not have to revisit it. Filed 2026-06-07 from the OQ-13 wizard-handbook-gap conversation. | S | 5 | APP-16, APP-28 |
| APP-30 | **Resolved 2026-06-08 (`2092cc7`/`27324ef`/`6dd85a6`): subagent-driven, 3 TDD task-commits on `main`, two-stage review each. (1) Promoted the image-only signal out of the test module into production as `ingest.extractors.pdf_has_embedded_images(path)` (non-pdf / unparseable / no-page -> False, never raises; True when a parseable PDF has >=1 embedded image) + 6 unit tests. (2) Refactored `test_corpus_integration.py` onto the promoted helper (deleted the local `_empty_extraction_is_explained`, single source of truth). (3) Added the screen-7 guard in `app/onboarding/retention_policy.py`: after `extract_text` and BEFORE the AI call, empty `text.strip()` blocks the proceed, picks a scan-specific vs generic "no readable text" message, wipes the staging dir, and re-renders the upload form; the early return means `extract_retention_bundle` is never run on empty text (no empty classifications scaffolded). 2 view tests assert the block + AI-never-called spy. Detect-and-warn only; OCR stays out of scope (INGEST-08). Suite 505 -> 513 (+8).** Screen-7 image-only-PDF guard. Onboarding screen 7 uploads a retention PDF and runs extraction *live*; a scanned / image-only PDF (no text layer) extracts to empty text and the wizard currently commits empty classifications and proceeds silently. Add a guard to the screen-7 handler (`app/onboarding/retention_policy.py`): detect empty extraction (reuse the `_empty_extraction_is_explained` image-only signal from `ingest/tests/test_corpus_integration.py`, INGEST-06), warn the admin the PDF looks scanned and needs a text layer, and block the proceed instead of scaffolding/committing empty classifications. Backend extraction-result handling, independent of the APP-28 retemplate. **Recommend landing first** — small, isolated, de-risks the live DISC demo (INGEST-06 found exactly this in 1 of PT's 19 corpus docs). Detect-and-warn only; OCR stays out of scope (INGEST-08 covers the ingest path post-freeze). Filed 2026-06-08 from the APP-28/29 readiness pass. | S | 5 | INGEST-06, APP-15 |

**Lane acceptance (week 4):** A new admin can complete the seven-screen wizard (including pointing PolicyCodex at the diocese's retention policy as a source-of-truth reference), ingest from a local folder of exported policies, see policies appear as drafts in the policy repo with retention values sourced from the reference document, edit a policy through the form (which opens a PR), have a reviewer approve via the UI, and publish (which merges the PR and triggers the handbook build).

## Publish Lane (P0.5)

| ID | Title | Size | Week | Depends on |
|----|-------|------|------|-----------|
| PUBLISH-01 | Prove Astro builds from a sample markdown directory (Astro picked per OQ-09 2026-05-11) | M | 1-2 | None |
| PUBLISH-02 | Chapter-section-item URL scheme and page templates | M | 2 | PUBLISH-01 |
| PUBLISH-03 | Default theme modeled on the LA handbook | M | 3 | PUBLISH-02 |
| PUBLISH-04 | Changelog page generated from `git log` of the policy repo | S | 3 | PUBLISH-02 |
| PUBLISH-05 | RSS feed generation | S | 3 | PUBLISH-02 |
| PUBLISH-06 | GitHub Actions workflow: build handbook on push to `main`, deploy artifact. **Resolved 2026-05-24 (`2ff22d4`): merged as build + `upload-pages-artifact` ONLY (no deploy; serving the artifact at the subdomain is PUBLISH-07). Vendored the Astro handbook + `build-handbook.yml` + a `sync-handbook.sh` re-vendor script into `repo-template/`; the workflow stages `policies/` into the handbook content dir, then `npm ci/build/verify`. Installed + live-smoked on `pt-policy` (green build, `github-pages` artifact produced from PT's real `document-retention` bundle). Design: `internal/superpowers/specs/2026-05-24-publish-06-handbook-build-workflow-design.md`.** | M | 3 | PUBLISH-01, REPO-04 |
| PUBLISH-07 | Serve the handbook at a public custom subdomain via GitHub Pages (DNS CNAME + org-level apex verification + Pages settings + three-job workflow with preflight gate + Astro site URL from Pages API + HOWTO Part 4 + live install on `pt-policy` at `handbook.ptdiocese.org`). Design: `internal/superpowers/specs/2026-05-26-publish-07-handbook-pages-serving-design.md`. **Resolved 2026-05-26 (`5d5cb4d`): live at `https://handbook.ptdiocese.org/`.** PolicyCodex `main` carries the shipping template (commits `2e58459`/`a2a04f7`/`75c3c95`/`9665663`/`3448edc`/`456d981` from the implementer + review-fix; `ba3ed7a` fix-forward for the preflight `pages:read` permission). `pt-policy` install took three Chuck-authorized admin-bypasses on ruleset `16256205`: PR #5 (`96b7ee8`, deploy SKIPPED), PR #6 (`dc86e52`, preflight fix), PR #7 (`1e0369c`, path-filter trigger nudge). Run `26451606202`: preflight + build + deploy all green; HTTP/2 200 with canonical URL; Let's Encrypt R12 cert through 2026-08-24; Enforce HTTPS already on (no day-2 step needed). Two follow-ups filed: handbook `<title>` still shows the generic "Diocese of Anytown" placeholder (diocese-name wiring is wizard-driven, APP-10..16 territory); and the `.github/`-only path-filter gap (Node-24 + PUBLISH-07 hit it) is REPO-12 (add `workflow_dispatch:` to the shipped workflows). | M | 4 | PUBLISH-06 |
| PUBLISH-08 | Vector chunk format export (architectural placeholder for v0.2 RAG) | S | 5 | PUBLISH-02 |

**Lane acceptance (week 4):** Given a merged PR on the diocese's policy repo, GitHub Actions builds and deploys a static handbook with chapter pages, individual policy pages, a `git log`-driven changelog, and an RSS feed to the configured subdomain within 5 minutes of merge.

## Critical Path

The longest dependency chain across lanes:

```
INGEST-01 (local folder reader)
  → INGEST-03 (file content extraction)
    → INGEST-04 (manifest)
      → AI-10 (inventory pass commits drafts)
        → APP-04 (GitHub provider)
          → APP-07 (edit form opens PR)
            → APP-17 (PR-state-to-gate)
              → APP-19 (publish merges PR)
                → PUBLISH-06 (Actions builds and deploys)
                  → first published handbook page on a real subdomain
```

Two blockers must clear in week 1, full stop:

- **App framework decision** (gates APP-01 and the entire App lane).
- **GitHub App registration plus PT diocesan policy repo creation** (gates APP-04, AI-10, and PUBLISH-06).

A third near-blocker, by Week 4: the **PT policy corpus exported to a local folder.** The 19 PDFs from the spike are enough to prove the path; the full demo corpus has to be on disk before INGEST-06 runs. This is data-handling, not authentication, and it can be done piecemeal as PT decides which private policies to include.

If any of the two true blockers slips past Friday of week 1, the timeline slips with it.

## Sprint Board Suggestions

- Use a kanban with five columns: Backlog, This Week, In Progress, Review, Done.
- One swimlane per coder lane (Ingest, AI, App, Publish) plus one for Cross-Cutting.
- Cross-lane dependencies show as blocked tickets tagged "depends on [TICKET-ID]."
- Stand-up cadence: 15-minute async update three times a week (Mon, Wed, Fri) plus a 30-minute lane-leads sync once a week.
- Ticket close requires acceptance criteria from `PolicyWonk-v0.1-Spec.md` checked off in the PR description.
- For the App lane specifically: every ticket landing should be exercised end to end against the PT policy repo by Friday of its week, since most App work is downstream of GitHub round trips.

## Open Decisions Tracked Outside the Tickets

These are spec-level open questions, not tickets. Resolve them in week 1:

- ~~License (REPO-01)~~ **Resolved 2026-05-11: AGPL-3.0.**
- ~~Web framework (APP-01)~~ **Resolved 2026-05-11: Python + Django.**
- ~~Static-site generator (PUBLISH-01)~~ **Resolved 2026-05-11: Astro.**
- Git operations library: shell out to `git` binary, or `libgit2` bindings (recommend shelling out for v0.1)
- Trademark and naming (is "PolicyCodex" available?) — open as OQ-02; TESS search pending.
- LA contact's role in the README (advisor, design reviewer, co-author) — open as OQ-05.
- PT diocesan leadership approval for the public handbook subdomain — open as OQ-06.
- ~~PT GitHub organization availability (or creation)~~ **Resolved 2026-05-11: `Diocese-of-Pensacola-Tallahassee`.**
